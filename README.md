
  # Site web pour diagnostiqueuse (copie)

  This is a code bundle for Site web pour diagnostiqueuse (copie). The original project is available at https://www.figma.com/design/o86n8O2nM28upruE0UoXCc/Site-web-pour-diagnostiqueuse--copie-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  